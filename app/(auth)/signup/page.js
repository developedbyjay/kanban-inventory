import { SignupForm } from "../../_components/auth/SignupForm";

export const metadata = {
  title: "Sign Up / Kanban Inventory",
  description: "Sign up for the Kanban Inventory Application",
};

export default function SignUpPage() {
  return <SignupForm />;
}
